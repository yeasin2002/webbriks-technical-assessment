name: webbriks-technical-assessment

services:
  postgres:
    image: postgres
    container_name: webbriks-technical-assessment-postgres
    environment:
      POSTGRES_DB: webbriks-technical-assessment
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
    ports:
      - "5432:5432"
    volumes:
      - webbriks-technical-assessment_postgres_data:/var/lib/postgresql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped

volumes:
  webbriks-technical-assessment_postgres_data:
